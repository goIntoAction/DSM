# 核心流程

## 启动流程

### 应用启动序列图

```mermaid
sequenceDiagram
    participant OS as Android System
    participant App as DSMApplication
    participant SM as SettingsManager
    participant DH as DsmApiHelper
    participant MA as MainActivity
    participant UI as Compose UI
    participant Nav as Navigation

    OS->>App: onCreate()
    App->>SM: init()
    App->>DH: initOkHttpClient() (同步)
    App->>DH: loadSessionAndInit() (异步)

    DH->>SM: 加载会话 (sid, cookie, synoToken)
    SM-->>DH: 返回会话数据
    DH->>DH: 创建 OkHttpClient
    DH-->>App: 初始化完成

    OS->>MA: onCreate()
    MA->>MA: enableEdgeToEdge()
    MA->>UI: setContent()
    UI->>UI: DSMTheme
    UI->>Nav: DSMNavHost

    Note over Nav: 默认起始页 = Login

    par 并行初始化
        UI->>DH: DsmApiHelper.init()
        MA->>MA: checkLaunchAuth()
    end

    MA->>SM: launchAuth.first()
    alt 启用生物识别
        MA->>MA: showBiometricAuth()
        Note over MA: 显示生物识别对话框
    else 禁用生物识别
        MA->>Nav: navigate(Login)
    end
```

### 关键初始化步骤

1. **DSMApplication.onCreate()**:
   - 初始化 `SettingsManager`（DataStore）
   - 同步初始化 `DsmApiHelper.initOkHttpClient()`
   - 异步加载会话 `DsmApiHelper.loadSessionAndInit()`

2. **MainActivity.onCreate()**:
   - 启用 Edge-to-Edge
   - 设置 Compose 内容
   - 初始化 `DSMNavHost`（默认起始页为 Login）
   - 监听会话过期事件

3. **生物识别启动认证** (可选):
   - 从 `SettingsManager.launchAuth` 读取设置
   - 如果启用，显示生物识别对话框
   - 认证成功后进入登录页

---

## 登录流程

### 登录序列图

```mermaid
sequenceDiagram
    participant User as 用户
    participant Screen as LoginScreen
    participant VM as LoginViewModel
    participant Repo as AuthRepository
    participant API as AuthApiRetrofit
    participant DSM as DSM Server
    participant SM as SettingsManager

    User->>Screen: 输入凭据
    Screen->>VM: sendIntent(LoginIntent.Login)
    VM->>Repo: login(host, account, password)
    Repo->>API: login(host, account, password)
    API->>DSM: POST /webapi/auth.cgi
    DSM-->>API: 返回 sid, synoToken
    API->>API: 解析响应
    API-->>Repo: 返回登录结果
    Repo->>SM: 保存会话 (sid, cookie, synoToken, host)
    SM-->>Repo: 保存成功
    Repo-->>VM: Result.success
    VM->>VM: updateState { isLoggedIn = true }
    VM->>VM: sendEvent(LoginEvent.NavigateToHome)
    VM-->>Screen: 状态更新
    Screen->>Screen: navigate(Home)
```

### 登录关键代码

```kotlin
// LoginViewModel.kt
class LoginViewModel @Inject constructor(
    private val authRepository: AuthRepository
) : BaseViewModel<LoginIntent, LoginState, LoginEvent>() {

    override suspend fun processIntent(intent: LoginIntent) {
        when (intent) {
            is LoginIntent.Login -> login(intent.host, intent.account, intent.password)
        }
    }

    private suspend fun login(host: String, account: String, password: String) {
        updateState { copy(isLoading = true) }
        val result = authRepository.login(host, account, password)
        result.onSuccess {
            updateState { copy(isLoading = false, isLoggedIn = true) }
            sendEvent(LoginEvent.NavigateToHome)
        }.onFailure {
            updateState { copy(isLoading = false, error = it.message ) }
            sendEvent(LoginEvent.ShowError(it.message))
        }
    }
}
```

---

## API 调用流程

### 带会话管理的 API 调用

```mermaid
sequenceDiagram
    participant VM as ViewModel
    participant Repo as Repository
    participant API as ApiRetrofit
    participant Client as OkHttpClient
    participant Interceptor as SessionInterceptor
    participant DSM as DSM Server

    VM->>Repo: loadData()
    Repo->>API: apiCall()
    API->>Client: execute()
    Client->>Interceptor: intercept()
    Interceptor->>Interceptor: 添加 Cookie 和 _sid
    Interceptor->>DSM: 发送请求
    DSM-->>Interceptor: 响应

    alt 会话过期 (code 106/107/109/409)
        Interceptor->>Client: 会话过期检测
        Client->>Repo: 返回会话过期错误
        Repo->>DH: DsmApiHelper.clearSession()
        DH->>DH: 发送 sessionExpiredEvent
        DH-->>VM: UI 监听后跳转登录
    else 正常响应
        Interceptor-->>VM: 返回数据
    end
```

### 会话拦截器

`SessionInterceptor` 自动为所有请求添加：
- Cookie（包含 sid）
- `_sid` 查询参数
- `SynoToken`（需要时）

---

## 文件下载流程

### 文件下载序列图

```mermaid
sequenceDiagram
    participant User as 用户
    participant Screen as FileScreen
    participant VM as FileViewModel
    participant Repo as FileRepository
    participant Service as DownloadService
    participant DB as Room Database
    participant DSM as DSM Server

    User->>Screen: 点击下载
    Screen->>VM: sendIntent(FileIntent.DownloadFile)
    VM->>Repo: downloadFile(path)
    Repo->>Service: 启动 DownloadService (前台服务)

    Service->>DSM: 创建下载请求
    DSM-->>Service: 返回下载流
    Service->>DB: 保存下载任务
    Service->>Service: 写入本地文件
    Service->>DB: 更新任务状态
    Service-->>VM: 下载完成
    VM->>VM: sendEvent(FileEvent.ShowToast("下载完成"))
```

---

## 状态管理流程

### MVI 状态流转

```mermaid
stateDiagram-v2
    [*] --> Idle
    Idle --> Loading: Intent
    Loading --> Success: API 成功
    Loading --> Error: API 失败
    Success --> Idle: 新 Intent
    Error --> Idle: 重试 Intent
    Success --> Loading: 刷新 Intent
```

### BaseViewModel 核心逻辑

```kotlin
abstract class BaseViewModel<S : BaseState, I : BaseIntent, E : BaseEvent> : ViewModel() {
    abstract val state: StateFlow<S>
    abstract val events: Flow<E>

    private val intentChannel = Channel<I>(Channel.UNLIMITED)

    init {
        viewModelScope.launch {
            intentChannel.consumeAsFlow().collect { intent ->
                processIntent(intent)
            }
        }
    }

    protected abstract suspend fun processIntent(intent: I)

    fun sendIntent(intent: I) {
        viewModelScope.launch { intentChannel.send(intent) }
    }
}
```

---

## 导航流程

### 主导航结构

```mermaid
graph TB
    Login[登录页] --> Home[主界面 Home]
    Home --> Dashboard[仪表盘 Dashboard]
    Home --> File[文件管理 File]
    Home --> Download[下载站 Download]
    Home --> Setting[设置 Setting]

    Dashboard --> Performance[性能监控]
    File --> FileDetail[文件详情]
    File --> FileSearch[文件搜索]
    File --> ShareManager[分享管理]

    Download --> TaskDetail[任务详情]
    Download --> PeerList[Peer 列表]

    Setting --> Preferences[偏好设置]
    Setting --> About[关于]
    Setting --> OtpBind[OTP 绑定]
```

### 导航切换逻辑

- 使用 `popBackStack()` 保持状态
- 如果目标路由不在栈中，使用 `navigate()`
- 已在目标路由时不做任何操作

---

*此文档由 AI README 分析生成*
